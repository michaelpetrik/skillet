# Skillet Refactor Plan

Last reviewed: 2026-05-14

## Goal

Refactor the Skillet TypeScript CLI toward clean architecture, SOLID, DRY, KISS, and YAGNI while preserving the current command behavior and packaging intent.

## Operating Rules

- Read `AGENTS.md` before changing code.
- Treat tracked and untracked files as user work. Do not run `git reset`, `git checkout`, `git clean`, or revert unrelated edits.
- Advance one open item at a time.
- Prefer characterization tests before moving behavior.
- Do not start broad module extraction or rewrites before R3 is complete; before then, limit work to tests, docs, guardrail metadata, release identity, and narrow safety fixes.
- Keep changes small enough to verify with local commands.
- Treat this file as Ralph's source of truth. Update status or evidence only when local evidence supports the status change.
- Every worker handoff must include `Done` and `TODO` checklists.

## Status Legend

- `[ ]` open
- `[~]` in progress
- `[x]` complete
- `[!]` blocked

## Quality Gate

Use this repo-owned command while the refactor is in progress:

```bash
npm run quality
```

The quality script includes offline security review with an all-files secret scan, typecheck, build, `npm test`, CLI smokes, package smoke, `sentrux check .`, and `sentrux gate .` when a reviewed `.sentrux/baseline.json` exists.

A backlog item is not complete unless its evidence column is satisfied by a local command, file diff, or explicit blocker note. Docs-only updates may verify with review and diff instead of the full gate.

## Current Baseline

- `npm run typecheck` passes.
- `npm run build` passes.
- `sentrux check .` passes against staged `.sentrux/rules.toml` constraints; score subdimensions, coupling, upward violations, and god-file detection remain observational until stricter post-baseline rules are reviewed.
- `sentrux gate .` now passes against reviewed `.sentrux/baseline.json`.
- `npm test` passes.
- Repo-owned quality, diff-aware secret scan, local security review, and activatable Git hook scripts exist; no static security lint exists.
- Repo-owned package smoke creates a local tarball, checks packaged paths against the intended allowlist, temp-installs it, and runs the packaged `skillet` bin.
- No `packageManager`, `.nvmrc`, `.node-version`, Volta, or mise runtime pin exists beyond `engines.node >=20`.
- `src/cli.ts` currently owns parsing and output; command dispatch, help selection, option validation, list/check/install/upgrade planning, and skills-add process execution now use dedicated helpers.
- `src/upload.ts` now builds an upload file-operation plan before applying live mutations, uses an injectable clock for changelog dates, and still owns upload rendering, filesystem writes, and temp cleanup; Git operations now run through an injectable command runner seam.
- `src/skill-files.ts` owns shared skill file constants and keeps catalog vs installed skill discovery as separate explicit traversal helpers.
- `package.json` now uses the scoped package name `@michaelpetrik/skillet`, keeps the installed binary as `skillet`, and is marked `private: true`; public release remains blocked until npm scope access and license/public access intent are explicitly reviewed.
- `CLAUDE.md` exists as a regular file even though `AGENTS.md` is canonical.
- Repo-owned Ralph scripts and resolver prompt exist, `bash -n` passes for them, a supervised Ralph preflight with `scripts/ci/run_quality_gates.sh` passed, a supervised `--execute` worker run passed, the cron watchdog is installed, Ralph treats a clean committed worktree as a hard handoff requirement, and the watchdog now targets a higher-level autoloop supervisor for unattended continuation.

## Target Shape

Minimum viable direction:

- Keep `src/cli.ts` as a thin executable wrapper or composition root.
- Move behavior that can be pure into small domain/application functions.
- Keep filesystem, Git, process execution, clock, and environment access behind explicit seams.
- Split upload into a pure plan and a side-effect executor.
- Validate user-controlled paths and process arguments at application boundaries before any side effect.
- Preserve argument-vector process execution; do not convert external commands to shell-interpolated strings.
- Introduce Sentrux rules in staged mode only after tests and directory/module boundaries exist, then promote to a reviewed baseline.
- Resolve release identity before any packaging smoke or publish path claims readiness.
- Avoid a DI container, class-heavy framework, or broad rewrite.

## Refactor Backlog

| ID | Status | Area | Task | Evidence | Resume Notes |
| --- | --- | --- | --- | --- | --- |
| R0 | [x] | Baseline | Decide whether the new CLI package files are accepted as the baseline and keep them tracked together. | 2026-05-13: `git status --short --untracked-files=all` reviewed; accepted the current CLI package/refactor bootstrap file set as the baseline to keep together. | Do not normalize unrelated files; keep the manifest, lockfile, source, docs, scripts, prompts, README, `.gitignore`, and `.sentrux` files grouped in the eventual tracking change. |
| R1 | [x] | Agent docs | Normalize `CLAUDE.md` to an `AGENTS.md` compatibility stub or remove it if compatibility is not needed. | 2026-05-13: `CLAUDE.md` contains exactly `@AGENTS.md`; `AGENTS.md` remains the only real instruction source. | Do not create new prose in `CLAUDE.md`. |
| R2 | [x] | Skills index | Sync `skills/README.md` with all `skills/**/SKILL.md` entries. | 2026-05-13: `find skills -path '*/SKILL.md'` and README link extraction both report the same 9 skill directories. | Keep future skill additions reflected in `skills/README.md`. |
| R3 | [x] | Tests | Add a minimal test harness and pure characterization tests for version policy, frontmatter behavior, and upload target validation scaffolding. | 2026-05-13: `npm test` passed with 9 Node test-runner tests covering `domain` version policy and `frontmatter`, including CRLF delimiter parsing. | Add tests first for any future behavior move. |
| R4 | [x] | CLI safety | Reject unknown long CLI options before side effects. | 2026-05-13: `npm test` covers `list --dryrun` non-zero, flag value rejection, and `list --help` success; 12 tests passed. | Keep behavior explicit for aliases and help. |
| R5 | [x] | CLI design | Define and implement a small command metadata/option schema only if it removes real duplication. | 2026-05-13: `npm test` passed 15 tests covering alias help and metadata-backed option parsing; `scripts/ci/run_quality_gates.sh` passed secret scan, typecheck, build, tests, CLI smokes, npm pack dry-run, and `sentrux check .`, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing. | Add future commands/options through the CLI metadata table so help, dispatch, and option validation stay aligned. |
| R6 | [x] | CLI use cases | Extract list/check/install/upgrade planning from presentation and process execution. | 2026-05-13: `src/cli-use-cases.ts` exports pure list/check/install/upgrade planners; `npm test` passed 19 tests including `test/cli-use-cases.test.mjs`; `scripts/ci/run_quality_gates.sh` passed secret scan, typecheck, build, tests, CLI smokes, npm pack dry-run, and `sentrux check .`, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing. | Keep future CLI use-case decisions in application helpers; R7 should introduce the runner seam without reintroducing spawnable planning into `src/cli.ts`. |
| R7 | [x] | Process seams | Introduce a command runner seam for `npx`, `bunx`, and Git operations. | 2026-05-14: `src/command-runner.ts` exposes an injectable command runner, skills-add command builder, and Git runner; `test/command-runner.test.mjs` simulates `npx`/`bunx`/Git success and failure and asserts raw argument arrays; `npm test` passed 25 tests; `scripts/ci/run_quality_gates.sh` passed secret scan, typecheck, build, tests, CLI smokes, npm pack dry-run, and `sentrux check .`, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing. | Keep future process execution behind `CommandRunner`; R8 should focus target path validation before mutations. |
| R8 | [x] | Upload safety | Validate `targetName` and canonicalize target paths before any write, delete, Git add, or dry-run report. | 2026-05-14: `test/upload-safety.test.mjs` covers empty, dot, absolute, traversal, newline, and control-character target names before live mutations, unsafe dry-run rejection, and canonical targets under `skills`; `src/upload.ts` and `skills/misc/publish-skill-to-skillet/scripts/publish_skill_to_skillet.py` validate target names and resolve targets under the skills root; `npm test` passed 33 tests; `python3 -m py_compile skills/misc/publish-skill-to-skillet/scripts/publish_skill_to_skillet.py` passed; a temp legacy dry-run rejected `--target-name '../escape'` without creating files; `scripts/ci/run_quality_gates.sh` passed, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing. | R9 should split upload planning from side effects without weakening the target safety checks; keep Git add paths sourced from validated `targetRelativeRoot`. |
| R9 | [x] | Upload planner | Split upload into pure planning plus side-effect application. | 2026-05-14: `src/upload.ts` builds an `UploadPlan` before live application and uses an injected clock for changelog dates; `test/upload-planner.test.mjs` covers dry-run/live parity, fixed-date changelog output, and no duplicate `CHANGELOG.md` entry on unchanged uploads; `npm test` passed 35 tests; `scripts/ci/run_quality_gates.sh` passed secret scan, typecheck, build, tests, CLI smokes, npm pack dry-run, and `sentrux check .`, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing. | R10 should reduce duplicated skill file discovery/constants while preserving installed vs catalog traversal differences. |
| R10 | [x] | File discovery | Share skill file constants and discovery helpers without building a generic framework. | 2026-05-14: `src/skill-files.ts` exports shared `SKILL.md`/`CHANGELOG.md` constants plus separate catalog and installed traversal helpers; `src/catalog.ts`, `src/installations.ts`, and `src/upload.ts` use the shared constants/helpers; `test/skill-files.test.mjs` covers catalog all-`SKILL.md` traversal and installed stop-at-skill-root traversal; `npm test` passed 37 tests; `scripts/ci/run_quality_gates.sh` passed secret scan, typecheck, build, tests, CLI smokes, npm pack dry-run, and `sentrux check .`, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing. | Keep future skill file traversal routed through the named helpers so installed and catalog semantics remain visible. |
| R11 | [x] | Sentrux | Tighten staged `.sentrux/rules.toml` after tests and module boundaries exist. | 2026-05-14: `.sentrux/rules.toml` now enforces `min_quality = 0.6`, `max_cycles = 0`, `max_cc = 50`, `max_fn_lines = 240`, and `max_file_lines = 1500` while documenting temporary allowances; `sentrux check .` passed and `scripts/ci/run_quality_gates.sh` passed with `sentrux gate` skipped because `.sentrux/baseline.json` is missing. | R12 should save a reviewed baseline from these staged rules; do not promote observational subdimension/coupling/god-file rules without baseline evidence. |
| R12 | [x] | Sentrux baseline | Save a reviewed structural baseline. | 2026-05-15: supervised run completed explicit baseline review flow outside normal workers: `sentrux check .`, `sentrux gate . --save`, then `sentrux gate .` passed; baseline saved at `.sentrux/baseline.json`. | Keep baseline refreshes as explicit reviewed actions; do not refresh broad baselines from unattended/autoloop workers. |
| R13 | [!] | Release identity | Resolve `package.json` publish identity, bin name, repository metadata, publish config, and license intent. | 2026-05-14: `npm view skillet name version description` shows unrelated `skillet@0.1.2`; `npm view @michaelpetrik/skillet name version description` returned E404; `package.json` and `package-lock.json` now use scoped `@michaelpetrik/skillet`, keep bin `skillet`, set `private: true`, and remove public `publishConfig`; README install and publishing notes match the scoped package/private state; `scripts/ci/run_quality_gates.sh` passed; `npm publish --dry-run` completed with tarball `michaelpetrik-skillet-0.1.0.tgz`. | Blocked on explicit owner decision: confirm access to the `@michaelpetrik` npm scope, choose and document license intent, then either keep the package private or remove `private` and add the intended `publishConfig.access`; do not publish public `UNLICENSED` unless intentional and documented. |
| R14 | [x] | Quality gates | Add a repo-owned quality script for typecheck, build, tests, pack dry-run, secret scan, and Sentrux when configured. | `scripts/ci/run_quality_gates.sh` exists and now runs offline security review, typecheck, build, `npm test`, CLI smokes, package smoke, and Sentrux check; `scripts/ci/pre_commit.sh`, `.githooks/pre-commit`, `.githooks/pre-push`, and `scripts/dev/install_git_hooks.sh` provide activatable local hook wiring with the changed-file secret scan before pre-commit tests and the full gate on pre-push; 2026-05-14: `bash -n scripts/ci/pre_commit.sh scripts/dev/install_git_hooks.sh .githooks/pre-commit .githooks/pre-push scripts/ci/run_quality_gates.sh scripts/ci/secret_scan.sh`, `npm run quality:precommit`, and `scripts/ci/run_quality_gates.sh` passed, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing; 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i05` replaced the standalone pack dry-run with `npm run package:smoke` inside `scripts/ci/run_quality_gates.sh`; `bash -n scripts/ci/run_quality_gates.sh scripts/ci/package_smoke.sh` passed and `scripts/ci/run_quality_gates.sh` passed with package smoke and `sentrux check .`, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing; 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i06` routed the full gate through `npm run security:review -- --offline`; `bash -n scripts/ci/run_quality_gates.sh scripts/ci/security_review.sh scripts/ci/secret_scan.sh` passed and `npm run quality` passed, reporting advisory audit as partial/skipped and `sentrux gate` skipped because `.sentrux/baseline.json` is missing; 2026-05-15: supervised `sentrux gate . --save` + `sentrux gate .` completed and `scripts/ci/run_quality_gates.sh` now executes `sentrux gate .` with baseline present. | Keep pre-commit fast and full package smoke + Sentrux gate in push/CI/release; keep advisory checks explicit (`--with-advisory`) when required. |
| R15 | [x] | Secret scan | Add a repo-owned diff-aware secret scan and local security review entrypoint before broader quality gates. | 2026-05-14: `scripts/ci/security_review.sh` added and exposed as `npm run security:review`; `bash -n scripts/ci/secret_scan.sh scripts/ci/security_review.sh` passed; `scripts/ci/secret_scan.sh --all` passed; temp fixture verification under `var/dev/ralph-autoloop/runs/20260514T072820Z-t-2026-05-14-ralph-autoloop/iterations/2/ralph/runs/20260514T073455Z-T-2026-05-14-autoloop-i02/secret-scan-fixtures` showed staged fake credential assignments fail with values omitted and `.env`, `.pem`, `.key`, and `.p12` paths are blocked; `npm run security:review -- --offline` passed with npm advisory audit reported partial/skipped. | Keep live registry/advisory checks explicit with `--with-advisory`; do not print secret values. |
| R16 | [x] | Packaging smoke | Verify package artifact from a clean build. | 2026-05-14: `scripts/ci/package_smoke.sh` builds a local tarball, checks packaged paths against an allowlist, temp-installs the tarball, and runs packaged `skillet --help` plus `skillet list --json`; `npm run package:smoke` and `scripts/ci/run_quality_gates.sh` passed. | Public release remains governed by R13; keep package smoke local until publish access and license intent are resolved. |
| R17 | [x] | Ralph loop | Verify the repo-owned Ralph checklist runner, watchdog, cron installer, and resolver prompt bound to this plan. | Ralph files exist; `bash -n` passes; non-execute preflight wrote `var/dev/ralph-checklist/runs/20260513T190455Z-T-2026-05-13-ralph-dev/status.json`; supervised `--execute` worker run wrote `var/dev/ralph-checklist/runs/20260513T200146Z-T-2026-05-13-ralph-dev/codex-final.md`; `scripts/automation/install_ralph_checklist_watchdog_cron.sh` installed two tagged crontab entries and a second run reported already up to date; 2026-05-14 worktree hygiene rules require Ralph to commit intended changes and leave clean status; `scripts/dev/ralph_autoloop.sh` now supervises repeated checklist iterations. | Cron can be disabled by setting `RALPH_WATCHDOG_ENABLED=false` in `var/automation/skillet-ralph-refactor-watchdog/watchdog.env`; dirty worktree blocks unattended spawn unless `RALPH_WATCHDOG_ALLOW_DIRTY_WORKTREE=true` is explicitly set; autoloop defaults to `RALPH_AUTOLOOP_MAX_ITERATIONS=6` and `RALPH_AUTOLOOP_PUSH=true`; keep `.sentrux/baseline.json` and broader refactor work as separate reviewed items. |

## Evidence Log

- 2026-05-13: Three read-only review waves found missing Sentrux rules, missing tests, fat CLI/upload modules, release identity blockers, and no repo-owned Ralph loop.
- 2026-05-13: Worker 23 tightened this plan with explicit acceptance criteria for security controls, release identity, staged Sentrux rollout, Ralph handoff, and the no-broad-rewrite-before-tests rule.
- 2026-05-13: Worker 23 observed concurrent Sentrux and Ralph bootstrap files; `sentrux check .` and `bash -n` for Ralph scripts pass, while Sentrux baseline and Ralph preflight remain pending.
- 2026-05-13: Main thread ran `scripts/ci/run_quality_gates.sh` successfully, then ran `scripts/dev/ralph_checklist.sh --task "Verify Ralph refactor loop setup" --quality-command "scripts/ci/run_quality_gates.sh"` successfully without `--execute`; cron was installed later after the supervised worker run.
- 2026-05-13: R0 baseline decision recorded after `git status --short --untracked-files=all`; current CLI package/refactor bootstrap files are accepted as one grouped baseline for future tracking.
- 2026-05-13: Main thread ran supervised `scripts/dev/ralph_checklist.sh --task "Advance the first unchecked item in docs/REFACTOR_PLAN.md without reverting user edits." --quality-command "scripts/ci/run_quality_gates.sh" --execute`; it completed R0 and reported a passing repo-owned quality gate in `var/dev/ralph-checklist/runs/20260513T200146Z-T-2026-05-13-ralph-dev/codex-final.md`.
- 2026-05-13: Main thread installed cron with `scripts/automation/install_ralph_checklist_watchdog_cron.sh`; `crontab -l | grep 'skillet-ralph-refactor-watchdog'` shows even/odd tagged entries, and a second installer run reported `cron entries already up to date`.
- 2026-05-13: Main thread completed R1 by replacing `CLAUDE.md` with the exact compatibility stub `@AGENTS.md`.
- 2026-05-13: Main thread completed R2 by adding `enforce-offline-gates` and `google-ai-studio-export-standardizer` to `skills/README.md`.
- 2026-05-13: Main thread completed R3 by adding `npm test` with Node's built-in test runner plus `test/domain.test.mjs` and `test/frontmatter.test.mjs`; `npm test` passed 9 tests.
- 2026-05-13: Main thread completed R4 by rejecting unknown long CLI flags and flag values; `npm test` passed 12 tests including CLI option behavior.
- 2026-05-13: Watchdog worker completed R5 by replacing duplicate CLI command/help/option switches with a small metadata table in `src/cli.ts`; `npm test` passed 15 tests and `scripts/ci/run_quality_gates.sh` passed, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-13: Watchdog worker completed R6 by moving list/check/install/upgrade planning into `src/cli-use-cases.ts` and adding `test/cli-use-cases.test.mjs`; `npm test` passed 19 tests and `scripts/ci/run_quality_gates.sh` passed, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Watchdog worker completed R7 by adding `src/command-runner.ts`, routing `skills add` and upload Git calls through the injectable runner seam, and adding `test/command-runner.test.mjs`; `npm test` passed 25 tests and `scripts/ci/run_quality_gates.sh` passed, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Main thread tightened Ralph worktree hygiene in `AGENTS.md`, `prompts/ralph-checklist-resolver.md`, `scripts/dev/ralph_checklist.sh`, `scripts/automation/ralph_checklist_watchdog.sh`, and Ralph docs: workers must commit intended changes before handoff, wrapper records `git-status.before` and `git-status.after`, and unattended watchdog spawn is blocked on dirty worktree.
- 2026-05-14: Main thread added `scripts/dev/ralph_autoloop.sh` and retargeted the watchdog to it, so unattended Ralph runs keep launching verified checklist iterations until blocked, complete, or max iterations are reached; `TODO` checklists remain audit handoff data rather than a user prompt.
- 2026-05-14: Autoloop iteration 1 completed R8 by adding upload target-name validation and canonical skills-root target resolution in both the TypeScript upload command and the packaged legacy publisher script; `npm test` passed 33 tests, a temp legacy dry-run rejected traversal without writes, and `scripts/ci/run_quality_gates.sh` passed with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Autoloop iteration 2 completed R9 by making upload derive dry-run and live results from an `UploadPlan`, applying file operations only in live mode, and injecting the changelog date clock; `npm test` passed 35 tests and `scripts/ci/run_quality_gates.sh` passed with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Autoloop iteration 3 completed R10 by moving skill file constants and catalog/installed discovery into `src/skill-files.ts`, wiring `src/catalog.ts`, `src/installations.ts`, and `src/upload.ts` to the shared module, and adding `test/skill-files.test.mjs`; `npm test` passed 37 tests and `scripts/ci/run_quality_gates.sh` passed with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Autoloop iteration 4 completed R11 by tightening `.sentrux/rules.toml` from bootstrap thresholds to staged rules for quality, cycles, complexity, function length, and file length; `sentrux check .` passed and `scripts/ci/run_quality_gates.sh` passed with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Autoloop iteration 5 blocked R12 rather than saving a baseline from a normal worker run; `sentrux check .` passed, while `sentrux gate .` failed because `.sentrux/baseline.json` is missing and instructed `sentrux gate --save` first.
- 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i01` advanced R13 by moving the manifest and lockfile from the taken `skillet` package name to scoped `@michaelpetrik/skillet`, keeping the binary as `skillet`, setting `private: true`, removing public `publishConfig`, and aligning README install/publishing notes; `scripts/ci/run_quality_gates.sh` passed and `npm publish --dry-run` reported tarball `michaelpetrik-skillet-0.1.0.tgz`; R13 remains blocked on explicit npm scope/license/access intent.
- 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i02` completed R15 by adding `scripts/ci/security_review.sh`, exposing it as `npm run security:review`, keeping the all-files secret scan mandatory, and marking npm advisory checks partial unless explicitly run with `--with-advisory`.
- 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i03` advanced R14 by adding `npm run quality`, `npm run quality:precommit`, `scripts/ci/pre_commit.sh`, `.githooks/pre-commit`, `.githooks/pre-push`, and `scripts/dev/install_git_hooks.sh`; `scripts/ci/pre_commit.sh` runs `scripts/ci/secret_scan.sh --changed` before `npm test`, and the pre-push hook runs `scripts/ci/run_quality_gates.sh`; `bash -n scripts/ci/pre_commit.sh scripts/dev/install_git_hooks.sh .githooks/pre-commit .githooks/pre-push scripts/ci/run_quality_gates.sh scripts/ci/secret_scan.sh`, `npm run quality:precommit`, and `scripts/ci/run_quality_gates.sh` passed, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i04` completed R16 by adding `scripts/ci/package_smoke.sh` and `npm run package:smoke`; the smoke builds and packs a local tarball, rejects unexpected tarball paths, temp-installs the artifact, and runs the packaged `skillet` bin help/list paths. `npm run package:smoke` and `scripts/ci/run_quality_gates.sh` passed.
- 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i05` advanced R14 by replacing the main quality gate's standalone `npm pack --dry-run` with `npm run package:smoke`, so pre-push/Ralph quality verifies the packaged tarball allowlist, temp install, and packaged CLI smokes; `bash -n scripts/ci/run_quality_gates.sh scripts/ci/package_smoke.sh` passed and `scripts/ci/run_quality_gates.sh` passed, with `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-14: Ralph worker `T-2026-05-14-autoloop-i06` advanced R14 by routing `scripts/ci/run_quality_gates.sh` through `npm run security:review -- --offline`, so the full pre-push/Ralph gate uses the repo-owned security review entrypoint while keeping live advisory checks explicit; `bash -n scripts/ci/run_quality_gates.sh scripts/ci/security_review.sh scripts/ci/secret_scan.sh` passed and `npm run quality` passed, with advisory audit reported partial/skipped and `sentrux gate` skipped because `.sentrux/baseline.json` is missing.
- 2026-05-15: Main thread completed R12 in supervised mode by running `sentrux check .`, `sentrux gate . --save`, and `sentrux gate .`; `.sentrux/baseline.json` is now present and gate comparison reports no degradation.
- 2026-05-15: Main thread completed R14 by rerunning `scripts/ci/run_quality_gates.sh` with baseline present so Sentrux check and gate both execute inside the repo-owned quality script.

## Deferred Or Blocked

- Do not publish under unscoped `skillet` unless the npm package ownership is resolved.
- Do not remove `package.json` `private: true` or publish publicly until npm scope access and license/public access intent are explicitly reviewed.
- Do not treat Sentrux as architecture enforcement until rules match the actual codebase and a reviewed baseline exists.
- Do not treat secret scanning, security review, or local hooks as enforced until repo-owned scripts and activation paths exist.
- Do not begin R5-R10 broad extraction work until R3 is complete and `npm test` is in the gate.
- Do not add a full DI container or class-heavy architecture framework.
- Do not rely on unattended cron as release evidence; it is an implementation aid and must remain disableable via `var/automation/skillet-ralph-refactor-watchdog/watchdog.env`.
