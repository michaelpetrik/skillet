interface:
  display_name: "Repo Quality Guardrails"
  short_description: "Audit or implement minimal honest repo guardrails"
  default_prompt: "Use $repo-quality-guardrails to classify the task as audit, proposal, or implementation; separate policy from real enforcement; prefer the smallest repo-owned guardrail change set; and verify GitNexus, hook wiring, secret scanning, docs gates, manifests, and Claude Code naming with local evidence."
